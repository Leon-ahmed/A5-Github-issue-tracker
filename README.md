- 1️⃣ What is the difference between var, let, and const?
Ans: These are used to declare variable. 
var is old style and global scope. After assining its  it's possible to reasign its value
let is modern style and block scoped. After assining its  it's possible to reasign its value
const is block scoped. After assining its vale become fixed and can't be changed




- 2️⃣ What is the spread operator (...)?
Ans: it is used to expand all elements of array and object.


- 3️⃣ What is the difference between map(), filter(), and forEach()?
Ans: map() function create a new array by transforming every element.
filter() it also create array with the elements that follows a given condition.
forEach() it is used to run a functiion for each element of an array.It does not return anything.




- 4️⃣ What is an arrow function?
Ans:its is a shorter mehtod of writing function in javascript. Example: const add=(a,b)=>a*b; it multiplies a with b


- 5️⃣ What are template literals?
Ans: It allows us to change variales value dynamically, throughout this we can create dynamic structure. Bactrick is used for doing this work.
example:
const name = "Leon";
console.log(`Hello ${name}`);
